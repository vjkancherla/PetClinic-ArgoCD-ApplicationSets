# K3D Development Environment Credentials Template
# Copy this file to .env.credentials and fill in your actual values
# Usage: cp .env.credentials.template .env.credentials && source .env.credentials

# Docker Registry Credentials (REQUIRED)
export DOCKER_USERNAME="your-docker-username"
export DOCKER_PASSWORD="your-docker-token-or-password"
export DOCKER_EMAIL="your-email@example.com"

# GitHub Credentials (REQUIRED for ArgoCD GitOps)
export GITHUB_USERNAME="your-github-username"
export GITHUB_TOKEN="your-github-personal-access-token"

# K3D Configuration (OPTIONAL - defaults provided)
export CLUSTER_NAME="mycluster"
export NAMESPACE="jenkins"
export VOLUME_NAME="k3d-data"

# Network Configuration (OPTIONAL)
export K3D_SUBNET="172.19.0.0/16"

# Usage Instructions:
# 1. Copy this template: cp .env.credentials.template .env.credentials
# 2. Edit .env.credentials with your actual credentials
# 3. Source before running scripts: source .env.credentials
# 4. Run setup: ./k3d-setup.sh